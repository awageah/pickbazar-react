/**
 * `A.3 Coming Soon` — Social login is dormant in Kolshi S2. The component is
 * kept as a no-op so modals / layouts that still mount it continue to work;
 * the actual button is rendered in `login-form.tsx` in a disabled state.
 */
const SocialLogin = () => null;

export default SocialLogin;
